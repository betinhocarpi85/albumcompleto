// OneSignal removido — usando Web Push nativo via VAPID
// Este arquivo mantido para não quebrar imports existentes
export default function OneSignalProvider() { return null }
export async function loadOneSignal() {}
